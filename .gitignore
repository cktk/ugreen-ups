# Build artifacts (distribute binaries via GitHub Releases instead)
*.exe
ups-monitor

# Runtime config (user-editable low-battery settings, not source-controlled)
ups-monitor.json

# Go test / coverage output
*.test
*.out
coverage.*

# Editor / OS noise
.idea/
.vscode/
*.swp
*.exe~
*~
Thumbs.db
.DS_Store
